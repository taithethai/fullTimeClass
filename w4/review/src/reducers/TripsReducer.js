export default (trips = [], action) => {
  switch (action.type) {
    default:
      return trips;
  }
}

// {startingLocation: '123 fake st', endingLocation: '225 fake st', distance: '400 meters', date: 'today'}
